/* =========================
   General Layout
========================= */
body {
  background-color: #000;
  color: #fff;
  font-family: 'Press Start 2P', cursive;
  text-align: center;
  margin: 0;
  padding: 0;
}

/* =========================
   Language Selection Page
========================= */
.language-select h1 {
  color: #ff69b4;
  margin-top: 60px;
  text-shadow: 0 0 10px #ff69b4, 0 0 20px #1DB954;
  font-size: 1rem;
}

.language-grid {
  display: flex;
  justify-content: center;
  flex-wrap: wrap;
  gap: 40px;
  margin: 50px auto;
  width: 90%;
  max-width: 900px;
}

.language-card {
  background: rgba(0, 0, 0, 0.6);
  border: 2px solid #00ff90;
  padding: 20px;
  border-radius: 10px;
  width: 120px;
  cursor: pointer;
  transition: transform 0.3s, box-shadow 0.3s;
}

.language-card img {
  width: 60px;
  height: 60px;
  margin-bottom: 10px;
}

.language-card p {
  font-size: 0.5rem;
  color: #fff;
}

.language-card:hover {
  transform: scale(1.1);
  box-shadow: 0 0 15px #00ff90, 0 0 30px #ff69b4;
}

/* =========================
   Lesson & Quiz Select Menus
========================= */
.language-card select {
  display: block;
  width: 100%;
  margin: 8px auto 5px;
  padding: 8px;
  font-size: 0.6rem;
  font-family: 'Press Start 2P', cursive;
  text-align: center;
  background: rgba(0, 0, 0, 0.7);
  color: #fff;
  border: 1px solid #1DB954;
  border-radius: 8px;
  cursor: pointer;
  transition: all 0.3s ease;
}

.language-card select:hover {
  border-color: #ff69b4;
  box-shadow: 0 0 8px #ff69b4, 0 0 12px #1DB954;
  background: rgba(30, 30, 30, 0.9);
}

.language-card option {
  background: #000;
  color: #fff;
  font-family: 'Press Start 2P', cursive;
}

/* =========================
   Header (XP + Level)
========================= */
header {
  width: 100%;
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 15px 30px;
  background: rgba(0, 0, 0, 0.3);
  position: sticky;
  top: 0;
  box-sizing: border-box;
  z-index: 100;
}

.xp-status {
  display: flex;
  align-items: center;
  gap: 10px;
}

.level-display {
  font-weight: bold;
  color: #1DB954;
}

progress {
  width: 120px;
  height: 15px;
  border-radius: 10px;
  overflow: hidden;
  background-color: #333;
}

/* =========================
   Centered Menu / Main
========================= */
main {
  text-align: center;
  margin-top: 60px;
}

.menu {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 15px;
  margin-top: 30px;
}

.menu button {
  width: 250px;
  padding: 15px;
  border: none;
  border-radius: 12px;
  background: #1DB954;
  color: white;
  font-size: 1em;
  font-weight: bold;
  cursor: pointer;
  transition: all 0.3s ease;
}

/* Glow Hover Animation for Menu Buttons */
@keyframes glow {
  0% { box-shadow: 0 0 5px #1DB954, 0 0 10px #1DB954; }
  50% { box-shadow: 0 0 20px #1DB954, 0 0 40px #1ed760; }
  100% { box-shadow: 0 0 5px #1DB954, 0 0 10px #1DB954; }
}

.menu button:hover {
  background: #1ed760;
  transform: scale(1.05);
  animation: glow 1.5s infinite;
}

/* =========================
   Footer
========================= */
footer {
  margin-top: auto;
  width: 100%;
  padding: 15px;
  text-align: center;
  background: rgba(0, 0, 0, 0.3);
  color: #ccc;
  font-size: 0.9em;
}

/* =========================
   Modular Lessons / Quiz Pages
========================= */
.quiz-container, .lesson-content {
  max-width: 800px;
  margin: 40px auto;
  padding: 25px;
  background: rgba(0,0,0,0.5);
  border-radius: 12px;
  box-shadow: 0 0 15px #1DB954;
}

.quiz-container h2, .lesson-content h2 {
  margin-bottom: 15px;
  color: #ff69b4;
  text-shadow: 0 0 5px #ff69b4, 0 0 10px #1DB954;
  font-size: 1rem;
}

.quiz-container button, .lesson-actions button {
  margin-top: 15px;
  padding: 12px 25px;
  border: none;
  border-radius: 10px;
  background: #1DB954;
  color: #fff;
  font-weight: bold;
  cursor: pointer;
  transition: all 0.3s ease;
}

.quiz-container button:hover, .lesson-actions button:hover {
  background: #1ed760;
  transform: scale(1.05);
}

/* Question choices */
.choice {
  display: block;
  margin: 10px auto;
  padding: 12px;
  width: 80%;
  max-width: 400px;
  border-radius: 10px;
  background: #1DB954;
  cursor: pointer;
  font-weight: bold;
  transition: 0.3s;
}

.choice:hover {
  background: #1ed760;
}

/* =========================
   Responsive Adjustments
========================= */
@media screen and (max-width: 768px) {
  .language-grid {
    gap: 20px;
  }
  .language-card {
    width: 100px;
    padding: 15px;
  }
  .language-card img {
    width: 50px;
    height: 50px;
  }
  .language-card p {
    font-size: 0.4rem;
  }
  .quiz-container, .lesson-content {
    width: 90%;
    padding: 20px;
  }
}
